// Root Prettier config — Prettier resolves upward from each file, so this one
// config covers every workspace. Values come from @ohmypos/config so the rule
// lives in one place (Playbook §13).
export { default } from '@ohmypos/config/prettier';
